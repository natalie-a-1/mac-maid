#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'

HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
SCRIPT="${HERE}/mac-maid.sh"

if [[ ! -f "$SCRIPT" ]]; then
  echo "✗ mac-maid.sh not found next to mac-maid" >&2
  exit 1
fi

exec "$SCRIPT" "$@"
